{
  "name": "soulfire-hi3",
  "version": "1.0.0",
  "main": "index.mjs",
  "license": "BSD-3-Clause",
  "type": "module",
  "dependencies": {
    "@google-cloud/functions-framework": "^3.3.0",
    "@types/cookie": "^0.6.0",
    "cookie": "^0.6.0",
    "node-fetch": "^3.3.2"
  },
  "scripts": {
    "compile": "tsc --project .",
    "deploy": "gcloud functions deploy hi3-validate-linked-roles --gen2 --region=us-west1 --runtime=nodejs20 --entry-point=invokeLinking --set-secrets=DISCORD_CLIENT_SECRET=DISCORD_CLIENT_SECRET:latest,CIPHER_KEY=CIPHER_KEY:latest,CIPHER_IV=CIPHER_IV:latest --trigger-http"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
